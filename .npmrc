# Suppress EBADENGINE warnings from deps that require Node >=22 (e.g. @mastra/core,
# p-retry, @openmeter/sdk). The project runs on Node 20 LTS and all functionality
# works correctly on that version; engine-strict would turn these into hard errors.
engine-strict=false
